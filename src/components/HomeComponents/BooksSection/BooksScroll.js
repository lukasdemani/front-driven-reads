import styled from 'styled-components';

const BooksScroll = styled.div `
    width: auto;
    overflow-x: scroll;
    display: flex;
    gap: 15px;
    flex-shirnk: 0;

    p {
        padding-bottom: 10px;
    }
`;

export default BooksScroll;