import styled from 'styled-components';

const SearchBox = styled.div`
    margin: auto;
    input {
        width: 80vw;
        heigth: 20px;

        border-color: #000000;
        border-radius: 5px;
    }
`;

export default SearchBox;